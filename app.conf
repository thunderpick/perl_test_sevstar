{
  'log'       => '/var/run/hypnotoad.log',
  'hypnotoad' => { 'listen' => ['http://*:80'] },
  'pg'        => $ENV{CMD_DB_URL},
   # || "postgresql://$ENV{POSTGRES_USER}:$ENV{POSTGRES_PASSWORD}@database/$ENV{POSTGRES_DB}"
}