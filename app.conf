{
  'log'       => '/var/run/hypnotoad.log',
  'hypnotoad' => { 'listen' => ['http://*:80'] },
  'pg'        => $ENV{POSTGRES_DSN} || spriontf(
    "postgres://%s:%s@database:5432/%s",
    $ENV{POSTGRES_USER},
    $ENV{POSTGRES_PASSWORD},
    $ENV{POSTGRES_DB},
  ),
}
